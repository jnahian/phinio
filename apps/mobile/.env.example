# Substitute your Mac's LAN IP so an iOS/Android device on the same Wi-Fi
# can reach the local web origin running on :3000.
# Find it with: ipconfig getifaddr en0
BETTER_AUTH_URL=http://192.168.1.x:3000
